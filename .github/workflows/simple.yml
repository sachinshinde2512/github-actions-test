name: Shell Commands

on: [push]

jobs: 
    run-shell-commands:
       runs-on: ubuntu-latest
       steps:
         - name: Echo Commands
           run: eccho "Hello World! This was my first bash command using GitHub Actions"
         - name: Multiline String
           run: |
               node -v
               npm -v